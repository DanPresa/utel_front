import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';

import UtelApp from './UtelApp';

ReactDOM.render(
	<React.StrictMode>
		<UtelApp />
	</React.StrictMode>,
	document.getElementById('root')
);
